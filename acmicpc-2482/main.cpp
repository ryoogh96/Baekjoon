#include <cstdio>
#include <bits/stdc++.h>

using namespace std;

int n,k;
int cache[1000][1000];

int colorCircle()
{

}


int main()
{
    memset(cache, -1, sizeof(cache));
    scanf("%d %d", &n, &k);

    return 0;
}
