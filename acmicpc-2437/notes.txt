출력
첫째 줄에 주어진 추들로 측정할 수 없는 양의 정수 무게 중 최소값을 출력한다.



예제 입력
7
3 1 6 2 7 30 1

예제 출력
21



방법 1)

모든 추의 합의 경우의 수를 다 구한다.
모든 추의 무게와 모든 합의 경우를 다 같은 자료형에 넣는다.

예제 케이스 : 1 ~ 37 (제일 작은수, 제일 큰수)

이 자료형에서 없는 숫자중 제일 작은 숫자를 출력하는게 바로 답이다.


How? 없는 숫자를 어떻게 판별하는가?

자료형을 정렬한다.

자료형의 위치와 i는 같지 않으므로 애초에 성립하지 않는다  
// 1 부터 37까지 for문을 돌리면서 i != 자료형(i) 이면 바로 출력한다. // 중복된 숫자가 있을 경우는? 
								     -> i와 자료형(i)의 위치가 흐뜨려진다.

/*
해결책 : 자료형을 따로 만들어서 추의 무게와 합의 경우 자료형 두개를 따로 만들고 따로 없는 숫자를 판별 한 뒤 
공통된 없는 숫자중 가장 작은 값을 출력한다.
*/



제일 작은수 부터 큰 수 까지 전부 배열에 넣은뒤 이를 for문 으로 자료형과 다 비교한 뒤 없는 제일 작은값을 for문으로
색출해낸다.

합의 경우의 수에서 max값을 색출한다.

int max = 0;
int min = 1;
vector<int> weight;
vector<int> ans;

if(weight.at(i) < min) min = weight.at(i);


if(sum.at(i) > max) max = sum.at(i); 

// 제일 작은수 부터 큰 수 까지 전부 배열에 넣은다.
for(int i = min; i < max; i++) ans.push_back(i);



2018/01/24

두개씩만 더한게 무게의 합의 모든 경우의 수가 아니라 세개씩도 더하고 네개씩도 더하고.... 끝이없다.

모든 경우의 수를 어떻게 구하는가



