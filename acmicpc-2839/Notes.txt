5킬로 설탕  3킬로 설탕   8킬로 = 5 + 3  15 킬로 = 5 * 3  10킬로 = 5 + 5


-1을 출력하기위해 정확하게 담을 수 있는 N킬로그램의 조건


5의 최소공배수 3의 최소공배수 5 + 3 = 8 의 최소공배수 5 * 3 = 15의 최소공배수

5의 최소공배수로 나누어졌을때 kg3 은 



6킬로 = 3 + 3


///////////////////////////////////////////////////////////


From source code NO.2797457

i = 3킬로그램의 갯수 n = 총 킬로그램 		5킬로그램은 일일이 for문에서 상수 5로 씀

구현 방법 :

loop(3킬로그램을 먼저 총 킬로그램에서 먼저 뺀다) 3킬로그램갯수++; ex) n(18) - i*=3 

#include<stdio.h>
int main()
{
	int chk=0,n; scanf("%d",&n);	//	Get Suger Kilogram
	for(int i=0;i<=n;i+=3){		//	
		if((n-i)%5==0){
			printf("%d",i/3+(n-i)/5);
			chk=1; break;	//	chk 로 -1판정
		}
	}
	if(!chk) printf("-1");
}


for 문에서 만약 n (킬로그램) 이 18일 경우	// i = 3킬로그램의 갯수;

for(int i=0;i<=n;i+=3){			
	if((n-i)%5==0){
		printf("%d",i/3+(n-i)/5);
		chk=1; break;

for i=0;				// 3킬로그램 0개

if((18-0)%5==0)				// 18%5!=0 ( =3 ) False
	printf("%d",0/3+(18-0)/5);	// 실행 안함 0 + 3 = 3 Output : 3
	chk=1; break;			// 실행 안함 if(!chk) true이되서 -1 출력

for i=3;

if((18-3)%5==0)				// True
	printf("%d",3/3+(18-3)/5);	// 1 + 3 = 4 Output : 4
	chk=1; break;			// if(!chk) false이되서 정상출력

for i=6;

if((18-6)%5==0)				// 12%5!=0 ( =2 ) False
	printf("%d",6/3+(18-6)/5);	// 실행 안함 2 + 2 = 4 Output : 4
	chk=1; break;			// 실행 안함 if(!chk) true이되서 -1 출력

for i=9;

if((18-9)%5==0)				// 9%5!=0 ( =4 ) False
	printf("%d",9/3+(18-9)/5);	// 실행 안함 3 + 1 = 4 Output : 4
	chk=1; break;			// 실행 안함 if(!chk) true이되서 -1 출력

for i=12;

if((18-12)%5==0)			// 6%5!=0 ( =1 ) False
	printf("%d",12/3+(18-12)/5);	// 실행 안함 4 + 1 = 5 Output : 5
	chk=1; break;			// 실행 안함 if(!chk) true이되서 -1 출력

for i=15;

if((18-15)%5==0)			// 3%5==0 (몫 0.6) True
	printf("%d",15/3+(18-15)/5);	// 5 + 1 = 6 Output : 6
	chk=1; break;			// if(!chk) false이되서 정상 출력

for i=18;

if((18-18)%5==0)			// 0%5==0  (0의 나머지도 0) True
	printf("%d",18/3+(18-18)/5);	// 실행 안함 6 + 0 = 6 Output : 6
	chk=1; break;			// if(!chk) false이되서 정상 출력

Total Output:

-1		// 3킬로그램이 0개일때	18 / 5 != 0
4		// 3킬로그램이 1개일때	15 / 5 == 0	5킬로 5개 3킬로 1개
-1		// 3킬로그램이 2개일때	12 / 5 != 0
-1		// 3킬로그램이 3개일때	9  / 5 != 0
-1		// 3킬로그램이 4개일때	6  / 5 != 0 
-1		// 3킬로그램이 5개일때	3  / 5 == 1	5킬로 0개 3킬로 5개
6		// 3킬로그램이 6개일때	0  / 5 == 0	5킬로 0개 3킬로 6개